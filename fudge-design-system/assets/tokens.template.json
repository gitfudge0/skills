{
  "$schema": "https://design-tokens.org/schema.json",
  "$description": "Source of truth. Everything downstream is emitted, never hand-edited. Replace every REPLACE marker; delete groups the archetype does not need rather than leaving them empty.",

  "primitive": {
    "$description": "Tier 1. Raw values, no meaning. Referencing these from product code is a lint error.",
    "neutral": {
      "$type": "color",
      "$description": "The ramp that carries most of the interface. Decide its temperature deliberately — warm, cool, or true — it sets the feel more than the accent does.",
      "0":   { "$value": "#REPLACE" },
      "25":  { "$value": "#REPLACE" },
      "50":  { "$value": "#REPLACE" },
      "100": { "$value": "#REPLACE" },
      "200": { "$value": "#REPLACE" },
      "300": { "$value": "#REPLACE" },
      "400": { "$value": "#REPLACE" },
      "500": { "$value": "#REPLACE" },
      "700": { "$value": "#REPLACE" },
      "900": { "$value": "#REPLACE" },
      "950": { "$value": "#REPLACE" }
    },
    "accent": {
      "$type": "color",
      "$description": "Tune per hue rather than stepping lightness mechanically — mechanical ramps read muddy in the mid tones.",
      "100": { "$value": "#REPLACE" },
      "300": { "$value": "#REPLACE" },
      "500": { "$value": "#REPLACE" },
      "700": { "$value": "#REPLACE" }
    },
    "space": {
      "$type": "dimension",
      "$description": "The base unit comes from the density axis of the extraction; every step is a multiple of it. Fixed count, fixed keys. The rules for which value applies where live in the docs, not here.",
      "1":  { "$value": "REPLACE — one base unit, the tightest gap in the system" },
      "2":  { "$value": "REPLACE — 2 base units" },
      "3":  { "$value": "REPLACE — 3 base units, the default control padding" },
      "4":  { "$value": "REPLACE — 4 base units, the default block gap" },
      "6":  { "$value": "REPLACE — 6 base units" },
      "8":  { "$value": "REPLACE — 8 base units, section separation" },
      "12": { "$value": "REPLACE — 12 base units" },
      "16": { "$value": "REPLACE — 16 base units, the largest gap the system allows" }
    },
    "radius": {
      "$type": "dimension",
      "$description": "Derived from the geometry axis of the extraction.",
      "sm": { "$value": "REPLACE" },
      "md": { "$value": "REPLACE" },
      "lg": { "$value": "REPLACE" },
      "pill": { "$value": "REPLACE — fully rounded; delete this token if the geometry has no fully-rounded form" }
    },
    "text": {
      "$type": "dimension",
      "$description": "Fixed number of steps. If a mock needs a size between two steps, the mock is wrong.",
      "2xs": { "$value": "REPLACE — smallest legible label; two steps below base" },
      "xs":  { "$value": "REPLACE — metadata and captions; one step below sm" },
      "sm":  { "$value": "REPLACE — secondary body, just under base" },
      "base":{ "$value": "REPLACE — 14px for dense tools, 16px for reading" },
      "md":  { "$value": "REPLACE — emphasised body, one step above base" },
      "lg":  { "$value": "REPLACE — smallest heading" },
      "xl":  { "$value": "REPLACE — section heading; ratio from the typographic contrast of the direction" },
      "2xl": { "$value": "REPLACE — display; how loud depends on how expressive the direction is" }
    },
    "duration": {
      "$type": "duration",
      "$description": "Derived from the motion character of the direction. A system with little motion keeps these short, or deletes the group.",
      "instant":  { "$value": "REPLACE — state flips that should read as immediate" },
      "fast":     { "$value": "REPLACE — hover and focus feedback" },
      "moderate": { "$value": "REPLACE — entrances and exits of small surfaces" },
      "slow":     { "$value": "REPLACE — the largest movement the direction permits" }
    },
    "font": {
      "$type": "fontFamily",
      "$description": "By role, not by name. One superfamily across all three roles is often the strongest answer — the character transfers even when the exact face is not licensable.",
      "display": { "$value": "REPLACE — headings, hero type" },
      "body":    { "$value": "REPLACE — running text, UI labels" },
      "mono":    { "$value": "REPLACE — code, tabular data, anything that must align" }
    },
    "weight": {
      "$type": "fontWeight",
      "$description": "A system shipping six weights uses three. Pick regular, one for emphasis, one for headings.",
      "regular":  { "$value": "REPLACE" },
      "medium":   { "$value": "REPLACE" },
      "semibold": { "$value": "REPLACE" },
      "bold":     { "$value": "REPLACE" }
    },
    "leading": {
      "$type": "number",
      "$description": "Line height is a function of measure and size, not a constant. Tight for display, generous for long-form reading.",
      "tight":   { "$value": "REPLACE — display sizes" },
      "snug":    { "$value": "REPLACE — headings, short lines" },
      "normal":  { "$value": "REPLACE — body text" },
      "relaxed": { "$value": "REPLACE — long-form reading, prose" }
    },
    "tracking": {
      "$type": "dimension",
      "$description": "Large type needs negative tracking, uppercase needs positive, body needs neither.",
      "tight":  { "$value": "REPLACE — large display sizes" },
      "normal": { "$value": "REPLACE — body text, usually 0" },
      "wide":   { "$value": "REPLACE — uppercase labels, small caps" }
    },
    "border": {
      "$type": "dimension",
      "$description": "Widths tie to the separation device from the extraction — if the system separates with rules rather than shadow, these carry more weight than elevation does.",
      "hairline": { "$value": "REPLACE" },
      "thin":     { "$value": "REPLACE — default control border" },
      "thick":    { "$value": "REPLACE — emphasis, selected state" }
    },
    "easing": {
      "$type": "cubicBezier",
      "$description": "Entrance and exit are not the same curve — using one for both is the most common motion mistake.",
      "standard": { "$value": "REPLACE — general-purpose, in-place transitions" },
      "entrance": { "$value": "REPLACE — decelerate; something arriving should slow into place" },
      "exit":     { "$value": "REPLACE — accelerate; something leaving should speed away" }
    },
    "opacity": {
      "$type": "number",
      "$description": "Disabled opacity is the only legitimate use of opacity on text — everything else should be a real colour token so contrast stays computable.",
      "disabled": { "$value": "REPLACE" },
      "muted":    { "$value": "REPLACE" },
      "scrim":    { "$value": "REPLACE — paired with semantic.color.overlay-scrim" }
    },
    "icon": {
      "$type": "dimension",
      "$description": "Stroke weight must optically match the body text weight or icons read as a different family.",
      "grid":   { "$value": "REPLACE — 20 or 24, the grid the icon set is built on" },
      "stroke": { "$value": "REPLACE" },
      "sm":     { "$value": "REPLACE" },
      "md":     { "$value": "REPLACE" }
    },
    "breakpoint": {
      "$type": "dimension",
      "$description": "Breakpoints come from where the content breaks, not from device names.",
      "sm": { "$value": "REPLACE" },
      "md": { "$value": "REPLACE" },
      "lg": { "$value": "REPLACE" },
      "xl": { "$value": "REPLACE" }
    },
    "container": {
      "$type": "dimension",
      "$description": "Prose is the measure rule that matters: ~65 characters per line, capped regardless of viewport.",
      "prose":   { "$value": "REPLACE — ~65ch, long-form reading" },
      "content": { "$value": "REPLACE — default page content width" },
      "wide":    { "$value": "REPLACE — data-dense layouts, tables" },
      "full":    { "$value": "REPLACE — edge-to-edge" }
    }
  },

  "semantic": {
    "$description": "Tier 2. The theming seam. Dark mode and alternate brands remap ONLY this tier.",
    "color": {
      "$type": "color",
      "canvas":         { "$value": "{primitive.neutral.50}",  "$description": "Page background behind all surfaces." },
      "surface":        { "$value": "{primitive.neutral.0}",   "$description": "Default raised surface — panels, cards, rows." },
      "surface-sunken": { "$value": "{primitive.neutral.25}",  "$description": "Recessed areas: read-only fields, footers." },
      "surface-hover":  { "$value": "{primitive.neutral.50}" },
      "rule":           { "$value": "{primitive.neutral.200}", "$description": "Default hairline separator." },
      "rule-strong":    { "$value": "{primitive.neutral.300}", "$description": "Control borders and hover states." },
      "text-primary":   { "$value": "{primitive.neutral.900}", "$description": "Must clear 4.5:1 on surface in every theme." },
      "text-secondary": { "$value": "{primitive.neutral.500}", "$description": "Must clear 4.5:1 — this is the pair that usually fails." },
      "text-tertiary":  { "$value": "{primitive.neutral.400}", "$description": "Hints only. Never load-bearing information." },
      "text-inverse":   { "$value": "{primitive.neutral.0}" },
      "text-disabled":  { "$value": "REPLACE", "$description": "The one place the 4.5:1 rule is formally exempt — still keep it legible, not decorative." },
      "link":           { "$value": "REPLACE", "$description": "Decide whether the system underlines. Colour alone to signal a link fails for colour-blind users." },
      "selection-bg":   { "$value": "REPLACE" },
      "selection-fg":   { "$value": "REPLACE" },
      "action-solid":   { "$value": "REPLACE", "$description": "Primary control fill. If colour is reserved for state, this is ink rather than a hue." },
      "focus":          { "$value": "REPLACE", "$description": "Must clear 3:1 against every surface it can appear on." },
      "overlay-scrim":  { "$value": "REPLACE" }
    },
    "state": {
      "$type": "color",
      "$description": "Delete this group entirely if colour does not carry meaning in this product. Each level needs foreground, background, and an edge colour, and each must pair with a non-colour signal.",
      "REPLACE-level-1": { "fg": { "$value": "#REPLACE" }, "bg": { "$value": "#REPLACE" }, "edge": { "$value": "#REPLACE" } },
      "REPLACE-level-2": { "fg": { "$value": "#REPLACE" }, "bg": { "$value": "#REPLACE" }, "edge": { "$value": "#REPLACE" } }
    },
    "elevation": {
      "$type": "shadow",
      "$description": "Tie levels to meaning, not blur radii. If the separation device is rules rather than shadow, keep these near-invisible.",
      "1": { "$value": "REPLACE" },
      "2": { "$value": "REPLACE" },
      "3": { "$value": "REPLACE" }
    },
    "z": {
      "$type": "number",
      "$description": "Named and exhaustive so nobody writes 9999. Ordering encodes a real decision — decide whether toasts sit above modals and why.",
      "sticky":   { "$value": 100 },
      "dropdown": { "$value": 200 },
      "overlay":  { "$value": 300 },
      "modal":    { "$value": 400 },
      "toast":    { "$value": 500 },
      "tooltip":  { "$value": 600 }
    },
    "focus": {
      "$type": "dimension",
      "$description": "Ring geometry is semantic — a global decision, not component-scoped. The focus COLOUR already lives at semantic.color.focus; this group is only the ring's width and offset.",
      "ring-width":  { "$value": "REPLACE" },
      "ring-offset": { "$value": "REPLACE" }
    },
    "chart": {
      "$type": "color",
      "$description": "Optional — delete this group unless the archetype renders charts. Palette construction rules (categorical vs sequential vs diverging, and the accessibility constraints on each) belong to the dataviz skill; this group only reserves the token names so charts do not invent their own colours.",
      "series": {
        "1": { "$value": "#REPLACE" },
        "2": { "$value": "#REPLACE" },
        "3": { "$value": "#REPLACE" },
        "4": { "$value": "#REPLACE" }
      },
      "axis":       { "$value": "#REPLACE" },
      "grid":       { "$value": "#REPLACE" },
      "annotation": { "$value": "#REPLACE" }
    }
  },

  "component": {
    "$description": "Tier 3. The density seam. Density modes remap ONLY this tier. Dimensions only — component-scoped colour tokens multiply without earning anything.",
    "control": {
      "$type": "dimension",
      "height-sm":  { "$value": "REPLACE" },
      "height-md":  { "$value": "REPLACE" },
      "pad-x":      { "$value": "{primitive.space.3}" },
      "radius":     { "$value": "{primitive.radius.md}" },
      "target-min": { "$value": "REPLACE", "$description": "44px baseline. A visually smaller control still needs the full target via padding or a pseudo-element." }
    },
    "table": {
      "$type": "dimension",
      "$description": "Delete if the archetype has no tables.",
      "row-height": { "$value": "REPLACE" },
      "cell-pad-x": { "$value": "{primitive.space.3}" },
      "cell-pad-y": { "$value": "{primitive.space.2}" }
    },
    "panel": {
      "$type": "dimension",
      "pad":    { "$value": "{primitive.space.4}" },
      "radius": { "$value": "{primitive.radius.lg}" }
    }
  },

  "$themes": {
    "$description": "Each theme overrides semantic only — and ALL of it: every token in every semantic group (color, state, focus, chart if kept) must be redeclared, not just the handful shown below. The dark entry here illustrates the override mechanism, not a template to ship as-is — a partial theme is a bug, not a starting point. If a theme needs to touch tier 1 or tier 3, the tiering is wrong.",
    "dark": { "semantic.color.surface": "#REPLACE", "semantic.color.text-primary": "#REPLACE" }
  },

  "$densities": {
    "$description": "Each density overrides component only.",
    "compact": { "component.control.height-md": "REPLACE", "component.table.row-height": "REPLACE" }
  },

  "$print": {
    "$description": "Print is a real target, not an afterthought. Remaps semantic colour to ink-safe values and declares page geometry. Delete this block if the system never prints. Hairline borders below ~0.5pt disappear on press. The screen type scale is in px; print is in pt — do not reuse px values directly.",
    "semantic.color.canvas":       "#REPLACE — white",
    "semantic.color.text-primary": "#REPLACE — true black",
    "semantic.color.rule":         "#REPLACE — must survive at 600dpi",
    "semantic.color.surface":      "REPLACE — backgrounds do not print by default; leave transparent unless deliberately forced",
    "page": {
      "size":   "REPLACE — e.g. A4 or Letter",
      "margin": "REPLACE"
    }
  }
}
